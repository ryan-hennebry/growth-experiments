# Plans Index — growth-experiments

Plan storage for **growth-experiments** — research → design → plan → review → revise → build → analyze → compound workflow artifacts.

## Structure

Plans live under `docs/plans/` with subdirectory organization:

- `active/` — current work in progress
- `completed/` — finished workflows (recent, accessible)
- `archive/` — old / superseded work

**Naming:** `<abbrev>-v<version>-<type>.md` (or `<date>-<slug>-<type>.md` for one-off sessions).

**Types:** design, plan, reviews, analysis, research, compound, session.

**Reviews:** single file per workflow version (`*-reviews.md`), latest round prepended at top.

## Abbreviations

| Abbrev | Topic |
|--------|-------|
| gea | growth-experiments |

## Active Work

_No active workflow._

## Completed

| Version | Artifacts |
|---------|-----------|
_None yet._
